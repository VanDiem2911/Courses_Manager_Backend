spring.application.name=coursemanagement

# MongoDB
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/coursemanagement}

# JWT
app.jwt.secret=${JWT_SECRET:coursemanagement-secret-key-2024-very-long-string-for-security}
app.jwt.expiration=86400000

# Server
server.port=${PORT:8080}
